# 🏆 Google Sheet + Apps Script 天梯排行榜建立與部署指南

本指南將帶您在 **5 分鐘內** 建立專屬的家庭/班級天梯排行榜雲端資料庫，讓所有家庭成員跨手機、平板、電腦即時同步積分與排名！

---

## 📋 步驟 1：建立 Google 試算表 (Google Sheet)

1. 開啟 [Google 雲端硬碟](https://drive.google.com/) 或 [Google 試算表](https://sheets.new)。
2. 建立一張新的試算表，左上角名稱取為：`TOEIC_Leaderboard`。
3. （可選）您可以手動在第一列填入以下 8 個標題（若不填，程式首次執行時亦會自動建立）：
   - `A1`: **Email**
   - `B1`: **Nickname**
   - `C1`: **DisplayName**
   - `D1`: **PhotoUrl**
   - `E1`: **Points**
   - `F1`: **MasteredCount**
   - `G1`: **StreakDays**
   - `H1`: **LastUpdated**

---

## ⚡ 步驟 2：開啟 Apps Script 並貼上程式碼

1. 在剛建立的 Google 試算表頂部選單中，點擊 **「擴充功能 (Extensions)」** -> **「Apps Script」**。
2. 進入編輯器後，將預設的 `function myFunction() {}` 全部刪除清空。
3. 打開專案目錄下的 [`gas/Leaderboard_Code.gs`](file:///d:/工具開發區/多益網頁遊戲專案/gas/Leaderboard_Code.gs) 檔案，將裡面的程式碼**全選並複製**，貼到 Apps Script 編輯器中。
4. 點擊頂部的 **💾 儲存專案**（或按 `Ctrl + S`）。

---

## 🚀 步驟 3：發布並部署為「網頁應用程式 (Web App)」

> ⚠️ **注意：此步驟的權限設定最為關鍵！請務必依照下列說明選取！**

1. 點擊右上角藍色的 **「部署 (Deploy)」** -> 選擇 **「新部署 (New deployment)」**。
2. 點擊齒輪圖示 ⚙️「選取類型」，選擇 **「網頁應用程式 (Web App)」**。
3. 在彈出視窗中設定：
   - **說明**：`v1.0 天梯排行榜 API`
   - **執行身分 (Execute as)**：選擇 **「我 (您的 Email)」** (預設即可)
   - **誰有存取權 (Who has access)**：⚠️ **請務必改選為「所有人 (Anyone)」**（這樣家庭成員的手機不需另外登入權限才能上傳和讀取榜單）。
4. 點擊右下角 **「部署 (Deploy)」**。
5. 首次部署時，Google 會要求授權存取：
   - 點擊 **「審查權限 (Review Permissions)」**。
   - 選擇您的 Google 帳號。
   - 若出現「Google 尚未驗證這個應用程式」，點擊左下角 **「進階 (Advanced)」** -> 點擊 **「前往 (不安全) (Go to TOEIC_Leaderboard (unsafe))」**。
   - 點擊 **「允許 (Allow)」**。
6. 部署完成後，畫面會產生一組 **「網頁應用程式網址 (Web app URL)」**（格式類似 `https://script.google.com/macros/s/AKfycb.../exec`）。
7. 點擊 **「複製」** 該網址！

---

## 🎮 步驟 4：將 API 網址貼入單字訓練營

1. 開啟單字訓練營網頁遊戲。
2. 點擊漢堡選單 ☰ 內的 **🏆 天梯排行榜** 按鈕（或點擊畫面底部的 📢 戰況快報膠囊進入天梯擂台）。
3. 點擊天梯擂台紫色頂部卡片右上角的 **⚙️ 設定圖示**。
4. 在彈出的視窗中，貼上剛剛複製的 `https://script.google.com/macros/s/.../exec` 網址並確認儲存。
5. 系統會立即提示 **「✅ 天梯榜 API 網址已儲存！即刻嘗試連線...」**！

---

## 🌟 運作機制與說明

- **全自動同步**：每當成員登入 Google 帳號完成測驗、賺取積分、精熟單字或簽到打卡時，前端系統會在背景自動上傳成績至您的 Google 試算表。
- **即時排名運算**：後端 GAS 伺服器會自動依「積分榜」、「單字榜」、「連續榜」即時計算前三名高低頒獎台與所有成員的戰力傷害百分比。
- **多成員家庭共享**：其他家庭成員只要在自己手機的遊戲天梯榜 ⚙️ 貼上同一組 Web App 網址，所有人就能在同一個擂台上即時爭霸！
